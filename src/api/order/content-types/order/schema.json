{
  "kind": "collectionType",
  "collectionName": "orders",
  "info": {
    "singularName": "order",
    "pluralName": "orders",
    "displayName": "Pedido",
    "description": "Pedido do cliente com snapshot dos itens e cobrança Pix Asaas"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "user": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "plugin::users-permissions.user"
    },
    "reference": {
      "type": "string",
      "required": true,
      "unique": true,
      "private": true
    },
    "checkoutIdempotencyKey": {
      "type": "string",
      "private": true
    },
    "checkoutIdempotencyScope": {
      "type": "string",
      "unique": true,
      "private": true,
      "description": "Valor técnico único formado por usuário e checkoutIdempotencyKey; o Strapi 5 não declara índices compostos neste content-type."
    },
    "checkoutIdempotencyFingerprint": {
      "type": "string",
      "private": true
    },
    "checkoutProcessingStatus": {
      "type": "enumeration",
      "enum": ["processing", "completed", "failed", "reconciliation_required"],
      "private": true
    },
    "checkoutProcessingLeaseUntil": {
      "type": "datetime",
      "private": true,
      "description": "Prazo de processamento; após expirar, o retry exige reconciliação e não repete a chamada externa."
    },
    "checkoutProcessingError": {
      "type": "string",
      "private": true
    },
    "items": {
      "type": "json",
      "required": true
    },
    "totalAmount": {
      "type": "decimal",
      "required": true
    },
    "status": {
      "type": "enumeration",
      "enum": ["pending", "paid", "expired", "cancelled", "failed"],
      "default": "pending",
      "required": true
    },
    "asaasPaymentId": { "type": "string" },
    "asaasCheckoutId": { "type": "string" },
    "asaasInvoiceUrl": { "type": "string" }
  }
}
